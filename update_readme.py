# -*- coding: utf-8 -*-
"""在 README.md 顶部插入 v3 章节，保留 v2 历史内容。"""
old = open("README.md", encoding="utf-8").read()

head = """# 首板连板概率模型 · v3 多维评分系统（Local Edition）

> A股首板/连板次日晋级概率评分模型，**纯本地运行、零算力依赖、免费数据源**。
> 当日收盘后跑一次 `python run_daily_v3.py`，输出明日连板概率榜（**每只股票附预测理由**）；用 `python v3_backtest.py bs_daily_v3.csv` 做一年历史回测验证。

## v3 重建（2026-09，回应"算法依据什么/概率同质化/消息面缺失"质疑）

### 六因子加权评分（权重由一年回测网格搜索得出，按训练/验证两段均衡选取）

| 因子 | 权重 | 依据 |
|---|---|---|
| 连板身位 | 30% | 1板16% / 2板30% / 3板43% 晋级率（一年实测） |
| 涨停频率（近10日涨停次数） | 20% | 反复攻击的强势股晋级率高 |
| 5日动量 | 15% | 主升节奏晋级率高 |
| **换手率（新）** | 15% | 换手<2%晋级率43% vs 全池20.7%——低换手=惜售锁筹 |
| **放量倍数（新）** | 10% | 缩量涨停24.8% vs 天量14.4% |
| 市场情绪 | 10% | 涨停家数分档 |

### 概率校准（重构，解决同质化）
概率 = **分身位（1/2/3/4+板）× 综合分分位** 的实测晋级率（calib_v3.json，近一年 27,439 个涨停样本）。同身位股票概率拉开 1.5-2.5 倍差距，不再"概率都一样"。

### 当日预测增强（盘口 + 消息面）
- **封单强度**：封板资金/流通市值（一字巨单 +1.5 分）
- **封板时间 / 炸板次数**：东财实时字段
- **消息面事件**：当日个股新闻关键词识别（重组/收购/业绩预增/中标/增持/AI/政策），含负面语境过滤（终止/亏损/减持等不加分）。例：新华传媒（收购界面财联社、一字板、封单41%）识别为"重组+一字"最强信号，排当日预测第 1。
- ⚠ 以上依赖当日实时数据，baostock 历史缓存无法覆盖 → **未进入一年回测**，概率上调幅度小（≤16%）并标注"盘口增强，未回测"。

## 回测结果（2025-06-02 ~ 2026-09-18，319 交易日，训练/验证 5:5）

| 策略 | v2 | **v3** | 训练/验证 |
|---|---|---|---|
| 全池 TOP1 | 49.53% | **57.05%** | 57.78% / 56.12% |
| 全池 TOP3 | 47.54% | **54.02%** | 57.04% / 50.12% |
| 低位池(1-3板) TOP1 | 47.65% | **56.74%** | 61.11% / 51.08% |
| 首板池 TOP1 | 25.08% | **35.42%** | 41.11% / 28.06% |
| 首板池 TOP5 | 23.40% | **26.60%** | 28.22% / 24.50% |

诚实说明：首板晋级二板基准仅 16.27%（Q5 高分位上限约 22%），v3 把首板 TOP1 从 25% 提到 35% 已是纯历史量价的显著提升；50%+ 需要当日盘口与消息面（已加入当日预测，无法历史回测）。**低位池（1-3 板）TOP1 已稳定 50%+**，这是当前主策略。

## 快速开始（v3）

```bash
pip install -r requirements.txt

# ① 一年历史回测（仓库已附 bs_daily_v3.csv）
python v3_backtest.py bs_daily_v3.csv

# ② 权重网格搜索（约 1-2 分钟）
python explore_v3_factors.py bs_daily_v3.csv

# ③ 生成概率校准表
python make_calib.py

# ④ 每日预测（收盘后跑；默认只做 1-3 板；输出概率榜 + 预测理由 + 网页）
python patch_v3_today.py            # 补拉当日涨停池行情（可选，v3 缓存缺当日时）
python run_daily_v3.py              # 默认取最近交易日
python run_daily_v3.py --date 2026-09-18 --top-n 10
python run_daily_v3.py --first-board   # 首板专项
python run_daily_v3.py --max-boards 5  # 放开到 5 板（默认 3）
```

产物：`daily_prediction_v3_YYYYMMDD.html`（预测网页：排名榜+概率+评级+事件+理由+回测说明）、`v3_report.html`（回测报告）、`calib_v3.json`（校准表）。

## 数据

- **baostock**（免费，无需 token）：全市场日线 6,185 只 × 近一年，含 `close/pct/isST/volume/amount/turn`（1,838,821 行）→ `bs_daily_v3.csv`
- **AKShare 东方财富**：涨停池（封板时间/封板资金/炸板/换手率/连板数/行业）、个股新闻（消息面事件）
- 涨停判定：非 ST 主板 9.8% / 创业板·科创板 19.8% / ST 5%/20%（按涨跌幅阈值本地计算）

## GitHub Actions

`.github/workflows/daily-prediction.yml`：每个工作日 16:30（UTC 8:30）自动补拉当日数据 → 跑 v3 预测 → 生成网页 → 部署 GitHub Pages（`public/index.html` 为最新一期）。

---

# 历史版本：v2（10 因子，保留）

"""

new = head + old
with open("README.md", "w", encoding="utf-8") as f:
    f.write(new)
print(f"README updated, {len(new)} chars")
